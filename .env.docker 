# .env.docker - Docker Environment Variables
# Skopiuj do .env.local dla środowiska lokalnego

# Symfony
APP_ENV=dev
APP_SECRET=e22cfc3ada37a21e653e873f4a595872

# Database MySQL
MYSQL_DATABASE=skybrokersystem
MYSQL_ROOT_PASSWORD=root
MYSQL_USER=skybroker
MYSQL_PASSWORD=Krasnoludek1!

# Database PostgreSQL (jeśli chcesz przejść na PostgreSQL)
POSTGRES_DB=skybrokersystem
POSTGRES_USER=skybroker
POSTGRES_PASSWORD=Krasnoludek1!

# Database URL dla Symfony (wybierz jeden)
DATABASE_URL=mysql://root:root@database:3306/skybrokersystem?serverVersion=8.0.32&charset=utf8mb4
# DATABASE_URL=postgresql://skybroker:Krasnoludek1!@postgres:5432/skybrokersystem?serverVersion=16&charset=utf8

# Redis
REDIS_URL=redis://redis:6379

# Mailer
MAILER_DSN=smtp://mailhog:1025

# Messenger
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0

# JWT Authentication
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=c0ffa907c75b325009c5ba4cdcb11550dc09fb8bddeb4783fccb76e7b8de5ad1

# Docker-specific
COMPOSE_PROJECT_NAME=skybroker
COMPOSE_FILE=docker-compose.yml

# Production settings (do użycia w GitHub Actions)
PROD_SERVER_HOST=your-production-server.com
PROD_SERVER_USER=deploy
PROD_SERVER_PATH=/var/www/skybrokersystem
DOCKER_REGISTRY=your-registry.com
DOCKER_IMAGE_NAME=skybroker